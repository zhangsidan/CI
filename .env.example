# Copy to .env to override any of these. Compose reads .env automatically.
# Every value has a default in docker-compose.yml, so .env is optional.
DB_PORT=3307
API_PORT=8080
DB_NAME=fxdb
DB_USERNAME=appuser
DB_PASSWORD=apppass
MYSQL_ROOT_PASSWORD=rootpass
